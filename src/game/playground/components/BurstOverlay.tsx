import React from 'react';
// DEPRECATED: BurstOverlay and BurstVisual are no longer used in the playground game.
// import { BurstVisual } from '../../../content/effector/BurstVisual';

// Overlay for burst effect visuals
// DEPRECATED: This component is no longer used. Use BurstEffectorVisual for burst effects.
/*
  burstEffects: any[];
// DEPRECATED: This component is no longer used. Use BurstEffectorVisual for burst effects.
/*
  // Defensive: always use array
  const safeEffects = Array.isArray(burstEffects) ? burstEffects : [];
  return (
    <svg
      style={{
        position: 'absolute',
        left: 0,
        top: 0,
        width: '100vw',
        height: '100vh',
        pointerEvents: 'none',
        zIndex: 9999,
      }}
      width="100%"
      height="100%"
    >
      {safeEffects.map(({ x, y, size, color, id }) => (
        // <BurstVisual key={id} x={x} y={y} radius={size / 2} color={color} />
      ))}
    </svg>
  );
*/
// This file is retained for reference only. Remove after migration is complete.

